<a href="/profile"><img class="profile-section__img profile"
    src="{{ data.url }}" alt="user profile picture" /></a>
<h2 class="profile-section__heading">
  <span class="profile-section__heading--main">{{ data.firstname }}
    {{ data.lastname }}</span>

  <!-- {{> _profile_sub_home }} -->
  {{> _profile_sub_profile }}
</h2>
